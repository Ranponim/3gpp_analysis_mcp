# 3GPP Analysis MCP - 환경 변수 설정 파일
# =============================================================================
# 이 파일은 애플리케이션의 환경 변수를 정의합니다.
# 실제 운영 환경에서는 민감한 정보를 포함하므로 .gitignore에 추가하세요.
# =============================================================================

# -----------------------------------------------------------------------------
# 애플리케이션 기본 설정
# -----------------------------------------------------------------------------
APP_NAME=3GPP Analysis MCP
APP_VERSION=1.0.0
APP_ENVIRONMENT=development
APP_DEBUG=true
APP_LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# 데이터베이스 설정 (PostgreSQL)
# -----------------------------------------------------------------------------
# 데이터베이스 연결 정보 (필수)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=3gpp_analysis
DB_USER=postgres
DB_PASSWORD=your_password_here

# 데이터베이스 연결 풀 설정 (선택사항)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30

# -----------------------------------------------------------------------------
# LLM (Large Language Model) 설정 - 로컬 LLM용
# -----------------------------------------------------------------------------
# 로컬 LLM 엔드포인트 설정 (필수)
LLM_ENDPOINTS=http://localhost:8000

# 로컬 LLM 모델 설정 (필수)
LLM_MODEL=Gemma-3-27B

# 로컬 LLM을 위한 기본값 설정 (API 키 불필요)
LLM_PROVIDER=local
LLM_API_KEY=

# LLM API 요청 설정 (선택사항)
LLM_MAX_TOKENS=4096
LLM_TEMPERATURE=0.2
LLM_TIMEOUT=180
LLM_MAX_RETRIES=3
LLM_RETRY_DELAY=1.0

# -----------------------------------------------------------------------------
# 백엔드 서비스 설정
# -----------------------------------------------------------------------------
# 백엔드 서비스 URL (필수)
BACKEND_SERVICE_URL=http://localhost:8000/api/v1

# 백엔드 서비스 설정 (선택사항)
BACKEND_TIMEOUT=30
BACKEND_MAX_RETRIES=3
BACKEND_AUTH_TOKEN=your_backend_auth_token_here

# -----------------------------------------------------------------------------
# 시간대 설정
# -----------------------------------------------------------------------------
APP_TIMEZONE=Asia/Seoul
DATA_TIMEZONE=UTC
DATETIME_FORMAT=%Y-%m-%d_%H:%M:%S
DATE_FORMAT=%Y-%m-%d
TIME_FORMAT=%H:%M:%S

# -----------------------------------------------------------------------------
# PEG (Performance Event Group) 계산 설정
# -----------------------------------------------------------------------------
PEG_DEFAULT_AGGREGATION=average
PEG_ENABLE_DERIVED=true
PEG_DEFAULT_TIME_WINDOW=1h
PEG_MAX_FORMULA_COMPLEXITY=100

# -----------------------------------------------------------------------------
# 로깅 설정
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
LOG_FILE_ENABLED=false
LOG_FILE_PATH=logs/app.log
LOG_CONSOLE_ENABLED=true

# -----------------------------------------------------------------------------
# 추가 환경변수 (ENV_SETTINGS.md에서 참조)
# -----------------------------------------------------------------------------
# 프롬프트 설정
DEFAULT_MAX_PROMPT_TOKENS=24000
DEFAULT_MAX_PROMPT_CHARS=80000
DEFAULT_SPECIFIC_MAX_ROWS=500
DEFAULT_MAX_RAW_STR=4000
DEFAULT_MAX_RAW_ARRAY=100

# 토큰 추정 설정
CHARS_PER_TOKEN_RATIO=3.5
FALLBACK_CHARS_PER_TOKEN_RATIO=4.0
PROMPT_TRUNCATE_BUFFER=200
STRING_SUMMARY_LIMIT=2000
PROMPT_PREVIEW_ROWS=200

# HTTP 설정
HTTP_USER_AGENT=Cell-Performance-LLM-Analyzer/1.0
MAX_PAYLOAD_SIZE_MB=1

# 타임존 설정
DEFAULT_TZ_OFFSET=+09:00
