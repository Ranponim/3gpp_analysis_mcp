# ===========================================
# Cell 성능 LLM 분석기 환경 설정 파일
# ===========================================
# 이 파일은 프로젝트 루트에 위치해야 합니다.
# 실제 값으로 변경한 후 사용하세요.
# ===========================================

# ===========================================
# 애플리케이션 기본 설정
# ===========================================
APP_NAME=3GPP Analysis MCP
APP_VERSION=1.0.0
APP_ENVIRONMENT=development
APP_DEBUG=true
APP_LOG_LEVEL=INFO

# ===========================================
# 데이터베이스 설정
# ===========================================
# PostgreSQL 데이터베이스 연결 정보
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_password_here

# 데이터베이스 연결 풀 설정
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30

# ===========================================
# LLM API 설정
# ===========================================
# LLM 제공업체 및 모델 설정
LLM_PROVIDER=local
# LLM_API_KEY=your_llm_api_key_here  # local provider에서는 필요 없음
LLM_MODEL=gpt-3.5-turbo

# LLM API 요청 설정
LLM_MAX_TOKENS=2000
LLM_TEMPERATURE=0.7
LLM_TIMEOUT=60
LLM_MAX_RETRIES=3
LLM_RETRY_DELAY=1.0

# LLM 엔드포인트 설정 (vLLM 서버용)
LLM_ENDPOINTS=http://10.251.204.93:10000,http://100.105.188.117:8888

# ===========================================
# 백엔드 서비스 설정
# ===========================================
# 백엔드 API 서비스 URL
BACKEND_SERVICE_URL=http://165.213.69.30:8000/api/analysis/results/

# 백엔드 요청 설정
BACKEND_TIMEOUT=30
BACKEND_MAX_RETRIES=3

# 백엔드 인증 설정 (선택적)
BACKEND_AUTH_TOKEN=
BACKEND_AUTH_HEADER=Authorization

# ===========================================
# 시간대 설정
# ===========================================
# 애플리케이션 및 데이터 시간대
APP_TIMEZONE=UTC
DATA_TIMEZONE=UTC

# 시간 형식 설정
DATETIME_FORMAT=%Y-%m-%d_%H:%M:%S
DATE_FORMAT=%Y-%m-%d
TIME_FORMAT=%H:%M:%S

# 기본 타임존 오프셋 (main.py에서 사용)
DEFAULT_TZ_OFFSET=+09:00

# ===========================================
# PEG (Performance Event Group) 설정
# ===========================================
# PEG 계산 및 분석 설정
PEG_DEFAULT_AGGREGATION=average
PEG_ENABLE_DERIVED=true
PEG_DEFAULT_TIME_WINDOW=1h
PEG_MAX_FORMULA_COMPLEXITY=100

# Choi 알고리즘 설정
PEG_USE_CHOI=false

# ===========================================
# 프롬프트 설정
# ===========================================
# 프롬프트 크기 제한
DEFAULT_MAX_PROMPT_TOKENS=24000
DEFAULT_MAX_PROMPT_CHARS=80000
DEFAULT_SPECIFIC_MAX_ROWS=500
DEFAULT_MAX_RAW_STR=4000
DEFAULT_MAX_RAW_ARRAY=100

# 토큰 추정 설정
CHARS_PER_TOKEN_RATIO=3.5
FALLBACK_CHARS_PER_TOKEN_RATIO=4.0
PROMPT_TRUNCATE_BUFFER=200
STRING_SUMMARY_LIMIT=2000

# ===========================================
# HTTP 설정
# ===========================================
# HTTP 요청 설정
HTTP_USER_AGENT=Cell-Performance-LLM-Analyzer/1.0
MAX_PAYLOAD_SIZE_MB=1

# HTTP 재시도 설정
LLM_RETRY_TOTAL=3
LLM_RETRY_BACKOFF=1.0

# ===========================================
# 테이블 및 컬럼 설정
# ===========================================
# 기본 테이블 및 컬럼명
DEFAULT_TABLE=summary
DEFAULT_TIME_COLUMN=datetime
DEFAULT_PEG_COLUMN=peg_name
DEFAULT_VALUE_COLUMN=value
DEFAULT_NE_COLUMN=ne
DEFAULT_CELL_COLUMN=cellid
DEFAULT_HOST_COLUMN=host

# ===========================================
# 로깅 설정
# ===========================================
# 로깅 기본 설정
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# 파일 로깅 설정
LOG_FILE_ENABLED=false
LOG_FILE_PATH=logs/app.log
LOG_FILE_MAX_SIZE=10485760
LOG_FILE_BACKUP_COUNT=5

# 콘솔 로깅 설정
LOG_CONSOLE_ENABLED=true
